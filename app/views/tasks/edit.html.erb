<%= notice %>

<%# エラーメッセージがあった場合に表示させる %>
<%= render partial: 'layouts/errors', locals: { task: @task } %>


<%# 編集フォーム %>
<%= render partial: 'form', locals: { task: @task, text: '編集',path: task_path(@task) } %>
